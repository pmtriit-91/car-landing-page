# Optional React Native Loading Policy

React Native skills hiện vẫn được giữ nguyên trong `skills/vercel-react-native-skills`.

Với web-only project, không load mặc định nhóm này để tránh nhiễu context.
Chỉ kích hoạt khi task liên quan React Native, Expo, mobile navigation, native UI hoặc mobile performance.
